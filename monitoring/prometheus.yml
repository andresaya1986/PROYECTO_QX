global:
  scrape_interval: 15s

scrape_configs:
  - job_name: api
    metrics_path: /prometheus-metrics
    static_configs:
      - targets: ["api:8000"]

  - job_name: ingestion
    metrics_path: /metrics
    static_configs:
      - targets: ["ingestion:9100"]
